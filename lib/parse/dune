(menhir
  (flags --external-tokens Lex.Token --explain)
  (modules myparser))

(library
  ; Rename this module to myparser to avoid name conflict with the standard library.
  (name myparser)
  (libraries base stdio lex)
  (preprocess (pps ppx_deriving.std)))